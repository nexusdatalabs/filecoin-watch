---
layout: "../layouts/Layout.astro"
---

import seriesData from "../data/daily_metrics.json";
import LineChart from "../components/LineChart.astro";
import DateFilter from "../components/DateFilter.astro";
import Header from "../components/Header.astro";

export const latestEntries = Object.entries(seriesData.at(0) ?? {});

<Header />

## Overview

<DateFilter />

<div class="charts" id="charts">
  <LineChart data={seriesData} x="date" y="unique_deal_making_clients" label="Unique deal-making clients" />
  <LineChart data={seriesData} x="date" y="unique_deal_making_providers" label="Unique deal-making providers" />
  <LineChart data={seriesData} x="date" y="clients_with_active_deals" label="Clients with active deals" />
  <LineChart data={seriesData} x="date" y="providers_with_active_deals" label="Providers with active deals" />
  <LineChart data={seriesData} x="date" y="providers_with_power" label="Providers with power" />
  </div>
